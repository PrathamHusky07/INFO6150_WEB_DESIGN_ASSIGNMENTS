# INFO_6105_WEB_DESIGN_ASSIGNMENTS

Part B
The navbar header tag used on the index.html page has numerous children and the entire container is styled using flex. The link tag is used to link the CSS file. The children include a few tags to display the text, an image, a video tag that plays the movie's trailer, and a button that takes the user to the checkout page. On the checkout page, the header element just contains the word checkout.
The index.html page contains two section tags. The audio tag as a child when clicked on plays an audio. The first tag is for the movie's description, and the second tag is for movie reviews.© Text and a background color are present in the footer.Users' email and credit card information are collected via a form on the checkout page, which also features a submit button.
